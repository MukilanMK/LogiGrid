version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: agent5
      POSTGRES_PASSWORD: agent5password
      POSTGRES_DB: profit_analytics
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
